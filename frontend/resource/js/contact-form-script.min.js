/**
 * @Author: Wang chunsheng  email:2192138785@qq.com
 * @Date:   2022-09-09 15:00:15
 * @Last Modified by:   Wang chunsheng  email:2192138785@qq.com
 * @Last Modified time: 2022-09-09 18:36:59
 */
$("#contactForm").validator().on("submit", function (event) {
    if (event.isDefaultPrevented()) {
        // handle the invalid form...
        formError();
        // submitMSG(false, "请检查填写内容");
    } else {
        // everything looks good!
        event.preventDefault();
        submitForm();
    }
});


function submitForm(){
    // Initiate Variables With Form Content
    var name = $("#name").val();
    var mobile = $("#mobile").val();
    var feedback = $("#feedback").val();


    $.ajax({
        type: "POST",
        url: "https://www.dandicloud.cn/api/user/relations",
        contentType: "application/json",
        headers: {
            'store-id':1,
            'bloc-id':1
        },
        dataType : "json",    
        data:JSON.stringify({
            name:name,
            contact:mobile,
            feedback:feedback
        }),
        success : function(res){
            if (res.code === 200){
                formSuccess(res.message);
            } else {
                formError();
                submitMSG(false,res.message);
            }
        }
    });
}

function formSuccess(message){
    $("#contactForm")[0].reset();
    submitMSG(true, message)
}

function formError(){
    $("#contactForm").removeClass().addClass('shake animated').one('webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend', function(){
        $(this).removeClass();
    });
}

function submitMSG(valid, msg){
    if(valid){
        var msgClasses = "h3 text-left tada animated text-success";
    } else {
        var msgClasses = "h3 text-left text-danger";
    }
    $("#msgSubmit").removeClass().addClass(msgClasses).text(msg);
}